server:
  port: 80
  

spring:
  servlet:
    multipart:
      max-file-size: 10MB
  datasource:
    name: test
    url: jdbc:mysql://119.3.20.110:3306/antifake
    username: JZR
    password: 123456
    # 使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat
    maxActive: 20
    initialSize: 1
    maxWait: 60000
    minIdle: 1
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxOpenPreparedStatements: 20
  #redis配置
  redis:
     host: 127.0.0.1
     database: 0
     password: 
     port: 6379
     timeout: 30000 
mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.antifake.model